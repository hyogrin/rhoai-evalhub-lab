# ============================================================
# RHOAI LMEval Builder Lab — Environment Configuration
# ============================================================
# Workbench: Run 0_setup/2_eval_hub_setup.ipynb Step 0
#            — edit values in the cell and run. .env is auto-created.
#
# Local dev: cp sample.env .env && edit .env
# ============================================================

# --- Required: Your OpenShift AI Project ---
NAMESPACE=your-namespace                      # <<< oc project name where model is deployed

# --- Required: Model Configuration ---
MODEL_NAME=your-model-name                    # <<< Model display name (e.g. "glm-53-flash")
MODEL_ENDPOINT=https://maas.example.com/v1    # <<< Model API endpoint (MaaS URL)
MODEL_API_KEY=                                # <<< API key for MaaS endpoint (auto-creates K8s Secret)

# --- Evaluation Settings ---
LIMIT=5                                      #     sample limit per benchmark (set higher for full eval)
BATCH_SIZE=8                                 #     batch size for evaluation

# --- EvalHub & MLflow ---
EVALHUB_URL=                                      #  external route or auto-derived from NAMESPACE
EVALHUB_AUTH_TOKEN=                               #  SA token (leave empty if you have oc login)
MLFLOW_TRACKING_URI=https://mlflow.redhat-ods-applications.svc:8443  #  auto-detected from MLflow CR

# ============================================================
# Example: MaaS endpoint
# ============================================================
# NAMESPACE=demo
# MODEL_NAME=glm-53-flash
# MODEL_ENDPOINT=https://maas.apps.ocp.cloud.rhai-tmm.dev/prelude-maas/glm-53-flash
# MODEL_API_KEY=sk-oai-xxxxx
# EVALHUB_URL=https://evalhub-demo.apps.cluster.example.com
