[project]
name = "rhoai-evalhub-lab"
version = "1.0.0"
description = "Korean LLM evaluation workshop using EvalHub on Red Hat OpenShift AI 3.5"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "tabulate",
    "jupyter",
    "ipykernel",
    "python-dotenv",
    "eval-hub-sdk>=0.1.6",
    "mlflow==3.13.0",
    "httpx>=0.27",
    "PyYAML",
]
